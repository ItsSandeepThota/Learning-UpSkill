# Auth Page Design

This component provides a clean Login/Register page using only Angular and plain CSS.

Files:
- `auth.component.ts` - standalone Angular component with mode switching
- `auth.component.html` - login/register UI
- `auth.component.css` - custom responsive styling without third-party libraries

Use `app-auth-page` as the selector when you wire it into your Angular routes or shell.